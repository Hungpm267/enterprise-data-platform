name: 'ecommerce_elt'
version: '1.0.0'
config-version: 2

profile: 'ecommerce_bigquery'

model-paths: ["models"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ecommerce_elt:
    marts:
      +materialized: table
