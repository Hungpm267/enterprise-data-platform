name: enterprise-data-platform
prefect-version: 3.0.0

build:

push:

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: .

deployments:
  - name: enterprise-deployment
    version: 1.0.0
    entrypoint: main.py:run_elt_pipeline
    description: "Automated Enterprise Data Platform ELT Deployment"
    work_pool:
      name: local-process-pool